import WrapOptions from "../wrap-options";
import NumderLicenses from "../length-licenses";
import Total from "../total";


const props = {
    container: '.container, .calc__box',
    components: [WrapOptions, NumderLicenses, Total]
}

export default props;